import streamlit as st
import numpy as np
import pandas as pd

# ======================================================
# ตั้งค่าหน้าเว็บ
# ======================================================
st.set_page_config(
    page_title="Equivalent Thickness (Odemark)",
    page_icon="🧱",
    layout="wide"
)

st.title("🧱 โปรแกรมคำนวณความหนาเทียบเท่า (Equivalent Thickness)")
st.markdown(
"""
พัฒนาเพื่อการเรียนการสอนและงานวิจัย  
**โครงสร้างผิวทางหลายชั้น (Multi-layer Pavement System)**  
โดยใช้แนวคิด **Odemark Transformation Method**
"""
)

# ======================================================
# Sidebar : การตั้งค่า
# ======================================================
st.sidebar.header("⚙️ การตั้งค่าโครงสร้าง")

n_layer = st.sidebar.slider(
    "จำนวนชั้นวัสดุ",
    min_value=3,
    max_value=5,
    value=3
)

n_exp = st.sidebar.number_input(
    "ค่า n (Odemark Exponent)",
    min_value=1.0,
    max_value=5.0,
    value=3.0,
    step=0.1
)

st.sidebar.info("งานผิวทางยืดหยุ่นมักใช้ n ≈ 3")

# ======================================================
# รับข้อมูลวัสดุ
# ======================================================
st.subheader("📥 ป้อนข้อมูลชั้นวัสดุ")

cols = st.columns(n_layer)

h = []
E = []

for i in range(n_layer):
    with cols[i]:
        st.markdown(f"### ชั้นที่ {i+1}")
        h_i = st.number_input(
            f"ความหนา h{i+1} (cm)",
            min_value=1.0,
            value=10.0,
            step=1.0
        )
        E_i = st.number_input(
            f"Modulus E{i+1} (MPa)",
            min_value=10.0,
            value=1000.0 / (i + 1),
            step=50.0
        )
        h.append(h_i)
        E.append(E_i)

h = np.array(h)
E = np.array(E)

# ======================================================
# คำนวณ Equivalent Thickness
# ======================================================
E_ref = E[0]

odemark_factor = (E / E_ref) ** (1 / n_exp)
h_eq = np.sum(h * odemark_factor)

st.divider()
st.subheader("📐 ผลการคำนวณ")

st.metric(
    label="ความหนาเทียบเท่า (hₑq)",
    value=f"{h_eq:.2f} cm"
)

st.caption("เทียบเท่าชั้นอ้างอิง (ชั้นที่ 1)")

# ======================================================
# ตารางสรุปข้อมูล
# ======================================================
df = pd.DataFrame({
    "ชั้นที่": np.arange(1, n_layer + 1),
    "ความหนา h (cm)": h,
    "Modulus E (MPa)": E,
    "ตัวคูณ Odemark": odemark_factor
})

st.subheader("📋 ตารางสรุปชั้นวัสดุ")
st.dataframe(df, use_container_width=True)

# ======================================================
# Sensitivity Analysis
# ======================================================
st.divider()
st.subheader("📊 Sensitivity Analysis")

delta = 0.10  # เพิ่ม E ทีละ 10%
h_eq_base = h_eq
sensitivity = []

for i in range(n_layer):
    E_new = E.copy()
    E_new[i] *= (1 + delta)

    h_eq_new = np.sum(
        h * (E_new / E_ref) ** (1 / n_exp)
    )

    S_i = ((h_eq_new - h_eq_base) / h_eq_base) / delta
    sensitivity.append(S_i)

df_sens = pd.DataFrame({
    "ชั้นที่": np.arange(1, n_layer + 1),
    "Sensitivity": sensitivity
})

st.dataframe(df_sens, use_container_width=True)

# ======================================================
# กราฟ (Cloud-safe)
# ======================================================
st.subheader("📈 กราฟแสดง Sensitivity ของแต่ละชั้น")

st.bar_chart(
    df_sens.set_index("ชั้นที่"),
    use_container_width=True
)

# ======================================================
# สรุปเชิงวิศวกรรม
# ======================================================
critical_layer = int(df_sens.loc[df_sens["Sensitivity"].idxmax(), "ชั้นที่"])
critical_value = df_sens["Sensitivity"].max()

st.success(
    f"📌 **ชั้นที่ {critical_layer} มีผลต่อ hₑq มากที่สุด** "
    f"(Sensitivity = {critical_value:.2f})"
)

st.markdown(
"""
### 🧠 ข้อสังเกตเชิงวิศวกรรม
- ชั้นบนที่มี **Modulus สูง** จะมีอิทธิพลต่อความหนาเทียบเท่าสูง
- ชั้นล่างแม้หนา แต่ถ้า E ต่ำ → ผลต่อ hₑq จำกัด
- ใช้ผลนี้อธิบายแนวคิด:
  - Structural Number (AASHTO 1993)
  - Layer Coefficient (aᵢ)
  - การปรับปรุงโครงสร้างอย่างคุ้มค่า
"""
)

st.info(
"""
📘 **ข้อเสนอแนะสำหรับการเรียนการสอน**
- ให้ นศ. เปลี่ยนค่า E ทีละชั้น แล้วอภิปรายผล
- เปรียบเทียบโครงสร้างหลายแบบที่ h รวมเท่ากัน
- เชื่อมโยงกับการออกแบบเชิงกล (Mechanistic Design)
"""
)
